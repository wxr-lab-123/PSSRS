<!--pages/payment/payment.wxml-->
<view class="container">
  <view class="payment-card">
    <view class="card-header">
      <text class="title">订单信息</text>
    </view>
    
    <!-- 订单信息 -->
    <view class="order-info" wx:if="{{orderInfo}}">
      <view class="info-item">
        <text class="label">订单号</text>
        <text class="value">{{orderInfo.orderNo}}</text>
      </view>
      <view class="info-item total">
        <text class="label">应付金额</text>
        <text class="value price">¥{{orderInfo.price}}</text>
      </view>
      <view class="info-item" wx:if="{{orderInfo.remark}}">
        <text class="label">备注</text>
        <text class="value">{{orderInfo.remark}}</text>
      </view>

      <view class="pay-section">
        <view class="section-title">支付方式</view>
        <radio-group class="method-group" bindchange="onMethodChange">
          <label class="method-item"><radio value="WECHAT" checked="{{selectedMethod==='WECHAT'}}"/> 微信支付</label>
          <label class="method-item"><radio value="ALIPAY" checked="{{selectedMethod==='ALIPAY'}}"/> 支付宝</label>
        </radio-group>

      </view>
    </view>
    
    <!-- 空状态 -->
    <view class="empty-state" wx:else>
      <text>订单信息加载失败</text>
    </view>
  </view>
  
  <view class="action-area" wx:if="{{orderInfo}}">
    <button class="btn-cancel" bindtap="goBack">取消支付</button>
    <button class="btn-pay" bindtap="handlePay">立即支付</button>
  </view>
</view>
